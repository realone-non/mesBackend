spring:
  datasource:
    url: jdbc:sqlserver://mes-dev.cyyxmbrc9eeg.ap-northeast-2.rds.amazonaws.com:1433;databaseName=MES_DEV;autoReconnect=true
    username: sa
    password: 123qweasd
    driver-class-name: com.microsoft.sqlserver.jdbc.SQLServerDriver
  jpa:
    hibernate:
      ddl-auto: create
    properties:
      hibernate:
        show_sql: true
        format_sql: true

  logging:
    level:
      org.hibernate.SQL: debug
      org.hibernate.type: trace

  h2:
    console:
      enabled: true
  server:
    port:80